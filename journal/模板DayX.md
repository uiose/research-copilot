---
template_version: 2
doc_type: day_log
ssot_role: daily_execution
week: "Week X"
day: "Day X"
date: ""
status_vocab:
  - 已完成
  - 部分完成
  - 未完成
  - 阻塞
research_output_vocab:
  - 对比实验
  - 失败案例记录
  - Prompt 修订日志
  - Related-work 笔记
  - 研究问题表述
evidence_type_vocab:
  - 命令
  - 文件
  - 输出
---

# Week X Day X

## 0) 模板说明
- 用途：本模板只处理日级执行，负责引用周规划中的 `G1-G3`，并留下可复用的完成证据与失败记录。
- 单向链路：周规划定义 -> 日记录引用并产证据 -> 周总结汇总并评价。
- YAML front matter：填写 `week`、`day`、`date` 等索引字段；共享词表也固定在 YAML 中，供后续程序读取。
- `[必填]` 字段构成当天最小闭环，`[可选]` 字段可以跳过。
- `关联目标ID` 只引用周规划里的 `G#`，不在这里重写周目标定义。
- `自由补充 [可选]` 仅保留上下文，不作为跨模板聚合字段。

## 1) 今日索引
- `week`、`day`、`date` 已在 YAML front matter 中填写。
- 关联目标ID [必填]：G1 / G2 / G3
- 承接点 [必填]：从上一个工作日或上周总结接过来的唯一输入接口
- 科研产出类型 [可选]：对比实验 / 失败案例记录 / Prompt 修订日志 / Related-work 笔记 / 研究问题表述

## 2) 今日任务块
### T1 [必填]
- 关联目标ID [必填]：G1 / G2 / G3
- 动作 [必填]：
- 预期产出 [必填]：
- 完成线 [必填]：
- 预计时长 [必填]：
- 资源 [可选]：

### T2 [可选]
- 关联目标ID：G1 / G2 / G3
- 动作：
- 预期产出：
- 完成线：
- 预计时长：
- 资源 [可选]：

### T3 [可选]
- 关联目标ID：G1 / G2 / G3
- 动作：
- 预期产出：
- 完成线：
- 预计时长：
- 资源 [可选]：

## 3) 过程事实
- 只写当天真实发生的动作、观察、卡点和中间变化，不在这里做周级结论。
- 

## 4) 完成证据
### E1 [至少 1 条，或在下方失败记录中明确写“今天无完成证据”]
- 关联目标ID [必填]：G1 / G2 / G3
- 证据类型 [必填]：命令 / 文件 / 输出
- 证据内容 [必填]：
- 落地路径 / 命令 [必填]：
- 说明 [可选]：

### E2 [可选]
- 关联目标ID：G1 / G2 / G3
- 证据类型：命令 / 文件 / 输出
- 证据内容：
- 落地路径 / 命令：
- 说明 [可选]：

## 5) 失败记录
### F1 [可选]
- 关联目标ID：G1 / G2 / G3
- 触发上下文/输入：
- 错误表现：
- 可能原因：
- 下一次验证：

### F2 [可选]
- 关联目标ID：G1 / G2 / G3
- 触发上下文/输入：
- 错误表现：
- 可能原因：
- 下一次验证：

## 6) 关键决策
- 这里允许自由叙述，但优先记录会影响后续执行或解释结果的决策。
- 

## 7) 事后收口
- 目标状态（按 G#）[必填]：例如 `G1=部分完成；G2=未开始；G3=阻塞`
- 今天实际推进 [必填]：
- 未解问题 [必填]：
- 次日首动作 [必填]：

## 8) 最低完成线
- 至少填完 `关联目标ID` 与 `承接点`。
- 至少定义 1 个任务块（通常为 `T1`）。
- 至少留 1 条 `完成证据`，或留 1 条有效 `失败记录`。
- 至少写 1 条 `次日首动作`，让今天可以收口到明天。

## 9) 自由补充 [可选]
- 只写有助于复盘当天情境的内容；这里不作为跨模板聚合字段。
