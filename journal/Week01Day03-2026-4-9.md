# Week 01 Day 03

## 一、事前规划
### 今日目标
- 对齐 Week 1 主线，在现有 baseline 基础上接入最小大语言模型调用
- 保持当前结构化输出协议不变，验证模型能否稳定生成 `topic`、`assumptions`、`next_step`
- 在不接入检索工具和 LangGraph 的前提下，形成最小 LLM 原型

### 成功标准
- 程序可以成功调用一次大语言模型
- 输出仍然是固定格式的结构化 JSON
- 当前程序仍然可以读取评测题集、统计类别并检查字段
- 今天结束时，可以清楚说明当前系统“已经接入 LLM，但还不是检索型 Agent”

### Todo
- [ ] 确定 Day 3 的最小模型调用方式
- [ ] 在项目中加入环境变量配置方式（如 `.env`）
- [ ] 修改 `run.py`，把手写 `build_result()` 逐步替换为模型生成
- [ ] 跑通至少一次真实调用并记录结果
- [ ] 把 Day 3 的关键过程写进 journal

### 当前假设
- Week 1 的重点仍然是形成稳定 baseline，而不是堆叠复杂能力
- Day 3 只需要验证“模型调用 + 结构化输出”这件事，不需要今天就接入检索工具
- LangGraph 应该放在后续阶段引入，而不是在最小 LLM 调用还没稳定前就提前接入

## 二、过程记录
### 我做了什么
- 

### 过程中遇到的问题
- 

### 我当时是怎么想的
- 

### 关键决策
- 

### 留下的证据
- 命令：
- 文件：
- 输出：

## 三、事后总结
### 今天实际完成了什么
- 

### 没完成什么
- 

### 今天学到的东西
- 

### 现在还不清楚的地方
- 

### 下一步
- 
### 明天的目标计划
- 
